un a deep diagnostic scan of the project to find out, with 100% certainty, why in PWA (standalone) mode — after completing a transfer — the bottom navigation and top header become scrollable.

✅ I don’t want a fix yet. I want an accurate technical explanation of what exactly is causing it.

Be extremely thorough and do not assume. I need confirmed proof from the code.

Here’s the situation:
	•	When doing a transfer → on the Processing screen, the page scrolls when it shouldn’t.
	•	After Success screen, the top header and bottom navigation also start moving (scrollable).
	•	When returning to the Dashboard, the scrollable state remains.
	•	This only happens in PWA standalone mode, never in normal browser view.

Your job:
	1.	Identify the exact element(s) that become scrollable — their selectors, parent containers, and CSS rules.
	2.	Find the code path or style rule that changes the overflow/height/positioning or DOM structure after transfer completion.
	3.	Confirm if this comes from:
	•	A route transition effect or layout re-render
	•	PWA-specific CSS (@media (display-mode: standalone))
	•	A leftover scroll-lock toggle (document.body.style.overflow, body classes, etc.)
	•	A component adding position: fixed, 100vh, or overflow:auto
	•	A service worker serving a stale CSS/JS version
	4.	Check both top header and bottom navigation specifically — find whether they are:
	•	Inside a scrollable ancestor
	•	Losing position: fixed or switching to relative/sticky
	•	Affected by new height or flexbox rules after success screen renders
	5.	Produce a clear cause summary that answers:
	•	What line of code or CSS rule triggers the scroll?
	•	Why does it only happen in PWA mode?
	•	What changes between browser vs standalone display-mode?
	6.	Include the proof: file name, line numbers, relevant diffs, computed CSS values, and the DOM chain that shows which container becomes scrollable.

⚠️ Do not modify or fix anything yet.
Just give me a Root Cause Report that’s certain — no guesses — including the evidence of what triggers the unwanted scroll for the top header and bottom navigation after a transfer in PWA mode.