// 📱 MASTER FIX PROMPT — Make Android UI & Behavior Match iOS Exactly

// 🔧 OBJECTIVE:
// Fix all Android-specific visual and behavioral issues based on the scan,
// while leaving iOS, backend logic, biometric system, sessions, and admin panel untouched.

// ✅ IMPLEMENTATION CHECKLIST:
// 1. Layout & Spacing:
//    - Adjust input field heights to render the same as iOS (override default browser styles)
//    - Increase button touch targets on Android to 48dp minimum (without affecting iOS)

// 2. Font Rendering & Scaling:
//    - Normalize font-weight rendering between Blink (Android) and WebKit (iOS)
//    - Apply consistent `line-height` and `text-rendering: optimizeLegibility`
//    - Add fallback font smoothing styles for Android Chrome

// 3. Form Field Issues:
//    - Override Android autofill background colors using `:-webkit-autofill` styles
//    - Standardize input focus ring appearance across platforms
//    - Make placeholder text consistent in color and opacity between Android/iOS

// 4. PWA Enhancements for Android:
//    - Add missing Android-specific meta tags in `manifest.json`:
//        → `shortcuts`, `theme_color`, `background_color`, `display: standalone`
//    - Apply proper Android status bar color (using `meta[name="theme-color"]`)
//    - Fix splash screen duration feel by minimizing heavy async loading at launch

// 5. Touch & Interaction Fixes:
//    - Add Material Design ripple/touch feedback for buttons on Android only
//    - Remove 300ms tap delay (`touch-action: manipulation`, `FastClick` if needed)
//    - Normalize scrollbar styling: hide on Android like iOS or match look
//    - Smooth scroll physics using consistent overflow settings

// 6. Viewport & Keyboard:
//    - Use `100dvh` for height instead of `100vh` to account for Android’s dynamic viewport
//    - Add `env(safe-area-inset-*)` support for Android safe areas
//    - Fix keyboard overlap issues with sticky headers/input padding

// ⚠️ DO NOT TOUCH:
// - Any backend logic (auth, sessions, biometrics)
// - iOS styling or layout
// - Admin panel features
// - Splash screen design or flow logic — only its feel/performance on Android

// 🔁 Final Goal:
// Android users should have the exact same visual experience, layout spacing, input behavior, tap feel, and PWA experience as iOS users.