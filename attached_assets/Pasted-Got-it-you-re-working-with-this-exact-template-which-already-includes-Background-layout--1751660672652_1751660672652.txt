Got it — you’re working with this exact template, which already includes:

✅ Background layout:
	•	“Bank of Ireland” logo
	•	Right-aligned Account Name / Number / Statement Period placeholders
	•	“ACCOUNT SUMMARY” header on left
	•	Horizontal blue title bar
	•	7 alternating rows (light green & white)
	•	Final light blue row at the bottom

This means the AI must overlay precise, dynamic content exactly inside these areas — not generating random placement.

⸻

🧠 FINAL SMART PROMPT (Optimized for your setup)

Use the existing template (already has all boxes and background visuals).
	1.	Top Right Placement:

	•	Populate:
	•	Account Name (line 1)
	•	Customer Number (line 2)
	•	Statement Period (line 3: Start Date – End Date)
	•	Position: top: 13%, right: 6%, right-aligned.
	•	Font: Arial, bold, size 14px.

	2.	Left Header Section:

	•	Fill in:
	•	“Balance on [Start Date]”
	•	“Total money in:”
	•	“Total money out:”
	•	“Balance on [End Date]”
	•	Place each on the left (under “ACCOUNT SUMMARY”) starting at top: 28%, left: 6%, spacing 32px apart.
	•	Font: Arial, regular, size 13px.
	•	Values right-aligned at approx left: 48%.

	3.	Table Row Mapping (7 rows + footer):

	•	Fill the table rows (green/white alternates) using real transaction history from the app, no random data.

Column	Left %	Width %	Alignment
Date	6%	15%	left
Description	22%	33%	left
Withdrawal	55%	15%	right
Deposit	70%	15%	right
Balance	85%	13%	right


	•	Format dates as DD/MM/YY, currency as €X,XXX.XX.
	•	Font: Arial, size 12.5px.
	•	Match colors from the template. No background changes.

	4.	Bottom Bar:

	•	Final row (“Ending Balance”) goes inside the light blue bar.
	•	Text: “Ending Balance” left-aligned at left: 6%.
	•	Balance value right-aligned at right: 5%, bold.

	5.	CRITICAL RULES:

	•	Only use visible space — no overflow.
	•	If there are fewer than 7 transactions, leave the extra rows blank.
	•	If more than 7, show only the last 7.
	•	Never invent fake transactions — only reflect actual data from the user’s transaction list.

✳️ You’re working within a fixed layout — match all placement exactly inside the colored bands. No AI guessing. No format deviation.

⸻

Let me know if you also want to add digital watermarking, authentication text, or PDF export instructions.