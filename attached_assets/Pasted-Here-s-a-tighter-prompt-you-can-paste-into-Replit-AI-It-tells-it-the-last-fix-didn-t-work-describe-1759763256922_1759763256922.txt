Here’s a tighter prompt you can paste into Replit AI. It tells it the last fix didn’t work, describes the exact regression (only on specific screens), and makes it diagnose + propose patches without changing code until you approve.

⸻

Prompt for Replit AI (diagnose + propose, don’t apply yet)

Context: Whatever you just fixed didn’t work. Normal main screens are fine (no scrolling), but:
	•	After confirming a transfer → Processing screen: I can scroll up.
	•	On Transfer Success screen: I can scroll up.
	•	When I tap Back to Dashboard: I can scroll up from the bottom navigation.
This is a regression. Previously, none of these screens allowed page scroll and the bottom nav stayed locked.

What I want (now):
Run a deep scan focused on these flows and identify the root cause. Provide a patch plan but do not modify files until I explicitly say “Apply the fix”.

Scope & suspects
	•	Screens/flows: TransferProcessing, TransferSuccess, Dashboard (after back navigation).
	•	Shell/layout: App root (html, body, #root/#app), main wrapper, router outlet, and BottomNav container.
	•	CSS/Tailwind: recent changes to overflow, height (100vh/h-screen vs 100dvh), position: fixed|sticky, safe-area insets, overscroll-behavior, padding-bottom for the nav, and any injected styles.
	•	JS: any scroll-lock helpers, document.body.style.overflow toggles, route transition effects, and cleanup handlers.

Deliverables (no code changes yet)
	1.	Diagnosis report (markdown):
	•	Identify the actual scroll container during each of the three cases above (document vs a wrapper). List element, classes, file:line, computed overflow-y, scrollHeight/clientHeight.
	•	Confirm whether BottomNav is inside a scrollable ancestor on those screens. It must not be. State its position, bottom, z-index, and whether safe-area padding is applied.
	•	Point to the exact diffs from the last few days that altered overflow/height/positioning (CSS and components).
	•	Note any route effects that set/unset scroll lock and whether cleanup runs on success/back navigation.
	2.	Repro steps (device/viewport + exact taps).
	3.	Patch plan (unified diffs, per file) to:
	•	Keep BottomNav fixed and outside any scrolling wrapper.
	•	Ensure Processing and Success screens either:
a) lock the root shell (overflow: clip|hidden on the non-scrolling shell) or
b) render as an overlay that prevents page scroll.
	•	Ensure Back to Dashboard resets any temporary overflow changes and scroll position, without re-enabling document scroll.
	•	Replace any 100vh/h-screen that’s causing extra height with 100dvh/min-h-dvh where appropriate.
	•	Add overscroll-behavior: none to the shell to stop scroll-chaining if needed.
	4.	Side-effects checklist (modals, toasts, keyboards on iOS/Android).

Acceptance criteria
	•	On Processing, Success, and Dashboard-after-Back: no page scroll, BottomNav remains locked to viewport bottom.
	•	Intended inner regions (lists/chats) keep their own scroll; nothing else scrolls.
	•	No cut-off content; layout responsive; safe areas respected.

Hints (use if relevant)
	•	If BottomNav sits inside a main/content wrapper that scrolls, move it outside the scrollable container (sibling to router outlet) and give it position: fixed; left:0; right:0; bottom:0; z-index:…; padding-bottom: env(safe-area-inset-bottom);.
	•	Ensure the app shell uses something like:
	•	html, body { height: 100%; }
	•	#app { min-height: 100dvh; display:flex; flex-direction:column; }
	•	.content { flex:1; overflow: clip; } (Only designated inner panes get overflow:auto.)
	•	If a scroll-lock manager exists, check it for imbalanced locks on success/back routes (cleanup in useEffect/unmount).
	•	If you previously added overflow:auto to body/shell to “fix” something, remove it and scope scroll to the intended inner region instead.

DevTools snippet (include results in the report)

Run this on each of the three screens and paste the table into the report:

(function () {
  const isScrollable = (el) => {
    const cs = getComputedStyle(el);
    const canScrollY = /(auto|scroll|overlay|clip)/.test(cs.overflowY);
    return canScrollY && el.scrollHeight > el.clientHeight;
  };
  const rows = [];
  document.querySelectorAll('*').forEach(el => {
    if (isScrollable(el)) {
      rows.push({
        node: el.tagName.toLowerCase() + (el.id ? `#${el.id}` : ''),
        classes: (el.className || '').toString().slice(0,120),
        overflowY: getComputedStyle(el).overflowY,
        scrollHeight: el.scrollHeight,
        clientHeight: el.clientHeight
      });
      el.style.outline = '2px dashed red';
    }
  });
  console.table(rows);
  return rows;
})();

Important: Output only the diagnosis, repro, and patch plan. Do not apply changes until I confirm.