Perfect — since you’ve already replaced the Pay Bills button with Statements, here’s the full prompt you need to give Replit AI to generate the proper statement using your background and overlay the correct data. Copy the entire thing below:

⸻

✅ Full Replit AI Prompt (Use Exactly This):

When the user taps the Statements button, generate a PDF overlaying account data on the provided Bank of Ireland statement background image.

Use the JPEG template as a fixed background, and overlay these sections using real values from the selected account (user.selectedAccountId):

⸻

🔹 Top-Right Info Block (under the Bank of Ireland logo):

Account Name: [user.fullName]  
Account Number: ****[last 4 digits of user.accountNumber]  
Statement Period: [startDate] to [endDate]

	•	Font: Helvetica or Arial
	•	Size: 10pt
	•	Bold only field titles (e.g., “Account Name:”)
	•	Align right and position in the white space beneath the logo, without touching the logo

⸻

🔹 Summary Section (under “ACCOUNT SUMMARY” on the left):

Opening Balance: €[openingBalance]  
Money In: €[totalDeposits]  
Money Out: €[totalWithdrawals]  
Closing Balance: €[closingBalance]

	•	Font: Helvetica, 10pt
	•	Left-aligned just under the “ACCOUNT SUMMARY” header
	•	Use consistent spacing and alignment exactly like the reference image

⸻

🔹 Transactions Table (within green rows of the template):

Loop through all transactions for the selected account. For each one, generate a row using this structure:

[Date] | [Description] | [Withdrawal] | [Deposit] | [BalanceAfterTransaction]

	•	Withdrawal and deposit should never appear together — only show one per row
	•	Font: Helvetica or Arial, 9pt
	•	Align all text inside the green-striped row boxes
	•	Bold any row headers if present (like “Previous Balance” or “Ending Balance”)

⸻

🔹 Footer (in the blue final row):

ENDING BALANCE                                 €[closingBalance]

	•	Font: Helvetica-Bold, 10pt
	•	Align this right in the final blue bar row
	•	Ensure spacing matches original template

⸻

Export everything as a downloadable PDF with the background locked, and all overlaid content rendered as vector text (not part of the image). This should ensure crisp printing and readability.

⸻

Let me know if you want me to help you wire this into a button action too.