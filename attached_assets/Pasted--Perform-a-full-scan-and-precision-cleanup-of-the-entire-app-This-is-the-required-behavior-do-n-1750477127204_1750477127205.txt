🔍 Perform a full scan and precision cleanup of the entire app. This is the required behavior — do not touch anything outside this.

⸻

🔐 ACCOUNT CREATION & SESSION STABILITY
	1.	Once a user signs up and verifies successfully, the account:
	•	Is saved permanently in the database
	•	Stays logged in forever, unless the admin deletes it from /admin/login
	•	dashboard only be accessed through biometric authentication
	•	If the user taps the logo again after being signed in, show: “You’re already signed in”

⸻

🔁 SESSION BEHAVIOR
	2.	After signup and first login:
	•	The user is always recognized as logged in
	•	Session state is stored and linked to the verified user account
	•	Sessions never expire or auto-reset unless manually deleted by admin
In admin/login and they press delete 
⸻

📱 APP BEHAVIOR (CLOSE VS MINIMIZE)
	3.	App must behave exactly like this:
	•	If the app is minimized, it should:
	•	Instantly resume where the user left off
	•	No splash screen, no login screen, no reload
	•	If the app is force closed (fully swiped up), then:
	•	Restart with splash screen → login screen
	•	User is still signed in and only needs to unlock with biometric
	•	After biometric auth, it goes to animation then straight to the dashboard

⸻

📶 OFFLINE FUNCTIONALITY
	4.	Make the entire app work fully offline after first login:
	•	Cache all assets, components, and local user data
	•	Don’t require internet to load splash screen, login page, dashboard, or saved state
	•	All session checks should work offline and respect biometric lock

⸻

🧠 DEEP CODE CLEANUP (NO MISTAKES)
	5.	Do a deep scan of every file in the codebase:
	•	Fix any:
	•	Misnamed variables, session keys, component IDs, function names, imports/exports
	•	Mismatched localStorage/session keys or DB fields
	•	Broken links between front and back end
	•	Delete only code that is:
	•	100% unused
	•	Not connected to any route, screen, session logic, or fallback
	•	Clearly broken or debug/dev-only
	•	Not related to: login, session, user data, dashboard, admin panel
	•	If unsure whether something is used: move it to a backup.js file — don’t delete it

⸻

🛑 DO NOT:
	•	Do not modify or break any working UI or navigation
	•	Do not touch payment logic, admin routes, or verified sessions
	•	Do not reset or rewire anything unrelated to this prompt

✅ End goal: Stable, secure, fully offline-ready app with clean, correct code, and permanent user login logic that matches this description exactly.